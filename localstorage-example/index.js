(function() {

  const elements = convertNodesToDomElements('data-id');
  console.log(elements);

  // criar um estado global para notes;
  // updateNotesListView();

  // function updateNotesListView()
    // getNotesFromLocalstorage();
    // se localStorage tiver itens:
      // append items no DOM:
        // <li class="list-group-item"></li>;
      // adiciona itens no estado;
      // esconder notesAlert = classList.add('d-none')
      // exibir notesList = classList.remove('d-none')
    
    
  // function getNotesFromLocalstorage():
    // checar localStorage por itens


  // formButton.addEventListener('click', () => {});
    // addNote();
      // no click, validar se formTextArea não está vazio.
      // se não, deve adicionar a nota ao estado e atualizar a view





})();

